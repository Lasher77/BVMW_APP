# Backend + Postgres via docker-compose
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/bvmw"
PORT=3005
LOG_LEVEL=info

# Authentication
WEBHOOK_AUTH_MODE=bearer
WEBHOOK_BEARER_TOKENS="sf:replace-me"
# Only required when `WEBHOOK_AUTH_MODE=hmac`
WEBHOOK_SHARED_SECRET="replace-me"
